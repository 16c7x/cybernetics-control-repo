---
version: 5

defaults:
  datadir: "data"

hierarchy:
  - name: "Yaml backend"
    data_hash: yaml_data
    paths:
      - "nodes/%{hostname}.yaml"
      - "datacentre/%{location}.yaml"
      - "domain/%{::domain}.yaml"
      - "common.yaml"
